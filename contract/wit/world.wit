package synod:agent@1.0.0;

world synod {
    import host:tenant/tenant-context@1.0.0;
    import host:interfaces/logging@2.1.0;
    import host:interfaces/kv-store@2.1.0;
    import host:interfaces/http-with-placeholders@2.1.0;
    import host:interfaces/signing@2.1.0;
    import host:interfaces/user-removal@2.1.0;
    import host:interfaces/user-profile@2.1.0;
    import host:interfaces/authorisation@2.1.0;
    import host:interfaces/clock@2.1.0;
    import host:interfaces/contracts-call@2.1.0;
    import host:outbox/outbox@1.0.0;

    export contracts;
}

interface contracts {
    record generic-input {
        input:        option<list<u8>>,
        user-profile: option<list<u8>>,
        context:      option<list<u8>>,
    }

    compose-action: func(req: generic-input) -> result<list<u8>, string>;
    get-trace:      func(req: generic-input) -> result<list<u8>, string>;
    evaluate:       func(req: generic-input) -> result<list<u8>, string>;
    execute:        func(req: generic-input) -> result<list<u8>, string>;
}
